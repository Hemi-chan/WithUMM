# WithUMM
MelonLoader에서 UnityModManager를 초기화해 `Mods` 폴더의 UMM 모드를 함께 실행하는 플러그인입니다.
Harmony 어셈블리를 버전별로 연결하고 UMM 패치 코드를 HarmonyX의 ILHook에 합성해 두 환경의 패치를 함께 적용합니다.

# AI Usage Disclosure
해당 플러그인은 일부 코드에 AI가 활용되었음을 알려드립니다.
